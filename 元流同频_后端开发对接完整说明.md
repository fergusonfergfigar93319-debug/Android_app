# 《元流同频》后端开发对接完整说明

**产品名称**：**元流同频**（工程目录 `TX_ku`；应用显示名与 `BrandConfig.appDisplayName`、`app_name` 一致）。  
**文档版本**：**V2.1（2026-04-21）**  
**客户端版本参考**：`app/build.gradle.kts` 中 `versionName` **1.0.1**（`versionCode` 2）  
**适用读者**：后端开发、测试、接口与网关负责人  
**文档目的**：根据 **当前 Android 客户端实现进度**，说明服务端需对齐的能力、**接口契约与客户端实际发送/解析的差异**、联调优先级与验收方式，便于排期与一次性对齐。

---

## 〇、与项目内其它文档的关系

| 文档 | 用途 |
|------|------|
| **`元流同频_后端接口联调手册.md`** | **接口路径、请求/响应示例、业务码、公开接口列表** 的详细参考（偏「契约说明书」）。 |
| **`TX_ku_Android前后端对接说明.md`** | Android 模型字段、审核语义、流式约定等客户端侧细纲（若与本文冲突，以 **仓库当前代码 + 本文 §二** 为准）。 |
| **`同频搭_API与数据定义文档.md`** | 历史表结构、部分路径与错误码示例。 |

**后端联调时建议**：以 **`元流同频_后端接口联调手册.md`** 为路径与字段大全，以 **本文 §二「契约对齐」** 为与 **当前 App 代码** 必须一致的硬约束。

---

## 一、客户端当前实现进度（截至 2026-04-21）

### 1.1 已落地的工程能力

| 维度 | 现状 |
|------|------|
| **HTTP 栈** | 已接入 **OkHttp + Retrofit + Gson**，集中在 `AppContainer`：`HttpLoggingInterceptor`（Debug 打 BODY）、`AuthInterceptor`、`TokenAuthenticator`；另有两套 **长读超时关闭** 的客户端：`sseOkHttpClient`（带登录拦截器，用于同频搭原生 SSE）、`openAiGatewaySseClient`（仅日志，用于 OpenAI 兼容网关 Key）。 |
| **Base URL** | `ApiConstants.BASE_URL` 占位为 `https://api.buddycard.com/api/v1/`；**`AppContainer` 中 Retrofit 已通过 `ApiConstants.baseUrlNormalized()` 与该常量对齐**（末尾自动补 `/`）。联调本地时请改 `BASE_URL` 为 `http://10.0.2.2:8000/api/v1/`（模拟器）或可达网关地址。 |
| **鉴权** | **登录**走 `AuthRepository` → `AuthApiService.login`；**刷新**走 `POST auth/refresh`（`TokenAuthenticator`）；Token 存 **DataStore**（`LoginSessionStore`）：`access_token`、`refresh_token`、`user_id`。 |
| **请求头** | 路径包含 `/auth/` 的请求 **不** 自动附加 Bearer（`AuthInterceptor`）；其余请求在已有 `Authorization` 头时 **不覆盖**（便于第三方网关单独带 Key）。 |
| **登录 UI** | `LoginScreen` → `AuthViewModel` → `AuthRepository`。视觉组件另有 `SpatialAcrylicAuth.kt` 等设计系统封装，路由入口仍以 `BuddyCardNavHost` 为准。 |
| **注册** | `RegisterScreen` 仍调用 **`LocalAuthRepository.register`（纯内存）**，**未**调用 `POST /auth/register`。接后端时需新增 Retrofit 接口并替换注册流程。 |
| **用户画像（读）** | 已实现 **`GET profiles/me`**：`ProfileApiService` + `ProfileRepository.fetchMyProfile`，`ProfileDto` / `BuddyCardDto` 按联调手册 **camelCase** 解析；成功业务码在 Repository 内 **同时接受 `20000` 与 `200`**（过渡双轨）。**元流档案页**由 `UserMeViewModel` 在已登录时自动拉取并刷新会话内存。 |
| **用户画像（写）** | **`POST /profiles`、`PUT /profiles/me` 尚未接 Retrofit**；`OnboardingViewModel` 等仍含 `TODO: POST /profiles`（建档提交仍偏本地 + 会话）。 |
| **业务模块** | 论坛、推荐、搭子申请、Feed 等 **仍以本地仓库/模拟数据为主**；代码中留有 `TODO`（如发帖全链路、搭子关系），**尚未用 Retrofit 全量接网**。 |
| **智能体流式** | **`AgentChatRepository` 已用 OkHttp SSE**（`EventSource` + `callbackFlow`）。默认请求 **`POST {BASE_URL}ai/agent/chat/stream`**（与 `ApiConstants.AGENT_CHAT_STREAM_PATH` 一致），**走 `sseOkHttpClient`，自动带登录 Bearer**（与联调手册 §5.5 对齐）。若 `BuildConfig.OPENAI_COMPAT_CHAT_URL` 非空，则改走 OpenAI 兼容 **`/v1/chat/completions` 式 SSE**，由 `OPENAI_COMPAT_API_KEY` 单独鉴权，**与同频搭后端解耦**。 |

### 1.2 登录演示开关（本地无后端时使用）

`AuthRepository.USE_MOCK_AUTH` 当前默认为 **`false`**（仓库已按真实联调方向默认关闭 Mock）。  
**无网关、纯 UI 演示**时可改为 **`true`**：不请求真实登录接口，直接写入 mock Token；此时 `ProfileRepository` 对远程 `profiles/me` 也会跳过或行为受限（见代码分支）。

### 1.3 合规与产品边界

第三方社区工具，非游戏厂商官方产品；资讯「官方」指 **产品内栏目**，非厂商本体（见 `BrandConfig` / `strings`）。

---

## 二、契约对齐（后端与客户端必须统一）

以下条目来自 **对照当前代码** 与 **`元流同频_后端接口联调手册.md`** 的差异。**未对齐将导致联调失败**。

### 2.1 统一响应 `code`：20000 与 200 双轨（模块不一致）

- **联调手册约定**：业务成功为 **`code == 20000`**。
- **登录**：`AuthRepository` 仅当 **`response.code == 200`** 且 `data != null` 视为成功（`LoginApiEnvelope`）。
- **刷新 Token**：`TokenAuthenticator` 同样以 **`envelope.code == 200`** 且 `data` 非空为成功。
- **拉取档案 `GET profiles/me`**：`ProfileRepository` 已写 **`code == 20000 || code == 200`**，与手册/历史网关更易兼容。

**建议**：后端统一输出 **`20000`**；客户端将 **登录、刷新** 的成功判断改为 **`20000`**（并与 `TokenAuthenticator` 一并修改），全局与手册一致。在改动前，网关需知晓 **登录/刷新仍可能按 200 判断**。

### 2.2 登录请求体字段名：`passKey` vs `password`

- **联调手册示例**：`password`。
- **当前客户端**：`LoginRequest` 使用属性名 **`passKey`**，Gson 默认序列化为 JSON 字段 **`"passKey"`**。

**后端可选**：同时接受 `password` 与 `passKey`，或要求客户端改为 `@SerializedName("password")`。联调前定一种。

### 2.3 登录响应体字段命名：camelCase vs snake_case

- **联调手册约定**：响应 `data` 内 **`accessToken`、`user`（camelCase）** 等。
- **当前客户端（登录）**：`LoginData` 使用 **`@SerializedName("access_token")`、`refresh_token`、`user_id`、`nickname`、`avatar_url`**，即按 **snake_case** 解析。

**档案接口**：`GET profiles/me` 的 `profile` / `buddyCard` / `agentTuning` 模型已按 **camelCase** 字段解析，**与手册 §3.2 一致**。

**后端可选**：登录响应继续 snake_case（与当前 App 一致），或输出 camelCase 并改客户端 Gson 模型。**禁止**两端假设不一致。

### 2.4 登录成功时 `data` 结构

当前模型需要（snake_case 形式）：

- `access_token`、`refresh_token`（刷新链路依赖 refresh_token）
- `user_id`、`nickname`、`avatar_url`（用于会话与展示恢复）

若后端采用手册中的嵌套 `user` 对象而无平铺字段，需扩展客户端模型或让后端 **同时给出平铺字段**，联调前约定。

### 2.5 刷新 Token

- **客户端**：`AuthApiService.refreshToken` → `POST auth/refresh`，**`application/x-www-form-urlencoded`**，字段名 **`refresh_token`**。
- **响应**：使用 `RefreshTokenApiEnvelope`，内含 `data.access_token`、`data.refresh_token`（当前仍为 snake_case 命名映射）。
- **Authenticator 行为**：仅当 HTTP **401** 等触发刷新；若刷新失败会 **`clearSession`**。业务 `code` 判定见 §2.1。

### 2.6 注册接口

手册中有 **`POST /auth/register`**。客户端 **尚未调用**。接入后注册成功建议返回与登录一致的 **双 Token + 用户信息**，以便注册后免二次登录。

### 2.7 智能体流式 `POST /ai/agent/chat/stream`

- 客户端默认对该 URL 使用 **JSON POST + `Accept: text/event-stream`**，正文与联调手册 §5.5 中 **同步/流式请求体约定** 一致（见 `AgentChatRepository` 内 `buddyAgentStreamBody`）。
- 需 **登录态**：与业务 API 共用 `AuthInterceptor`，应返回手册约定的 **`delta` / `done` / `error`** 事件语义（具体字段以联调手册为准）。

---

## 三、全局约定（后端建设时保持一致）

### 3.1 Base URL 与环境

| 环境 | 地址 |
|------|------|
| 本地 | `http://localhost:8000/api/v1` |
| Android 模拟器访问本机 | `http://10.0.2.2:8000/api/v1` |
| 生产（占位） | `https://api.buddycard.com/api/v1` |

路径与 **尾部斜杠** 需与网关、Retrofit `baseUrl` 习惯一致（Retrofit 要求 `baseUrl` 一般以 `/` 结尾）。

### 3.2 鉴权

需登录接口：`Authorization: Bearer <access_token>`。  
**公开接口**范围以 **`元流同频_后端接口联调手册.md` §1.4** 为准（注册、登录、部分 Feed、广场列表等）。

### 3.3 业务状态码、SSE、WebSocket

业务码表、AI 流式 **SSE** 事件、`ws` 私信格式等 **不再在本文重复**，以后端联调手册对应章节为准；客户端展示文案可与 `50301` 等对齐。

---

## 四、后端任务总览（按优先级）

与 **V1.0** 相比，任务条目仍适用；**变化**是：客户端侧 **登录/刷新骨架**、**`GET profiles/me` 拉档**、**原生智能体 SSE 调用** 已落地；后端需按 **§二** 对齐 **登录/刷新** 契约，并支撑 **注册、画像写入与全链路业务**。

### P0 — 最小闭环（可注册登录、发帖、公域列表）

| 序号 | 任务 | 与客户端关系 |
|------|------|----------------|
| P0-1 | **注册 / 登录 / Token + 刷新** | 客户端已实现 `login`、`refresh` 调用形态；**注册**待接；**成功码与 JSON 键名**见 §二。 |
| P0-2 | **用户画像** | **`GET /profiles/me`：客户端已接**；**`POST /profiles`、`PUT /profiles/me` 仍待客户端 Retrofit + 建档流程改造**（`OnboardingViewModel` 等 TODO）。 |
| P0-3 | **论坛公域列表** | `GET /posts`；仅 `APPROVED` 等可对陌生人展示的状态。 |
| P0-4 | **发帖** | `POST /posts`；返回 `moderationStatus`、`moderationHint`。 |
| P0-5 | **帖子详情权限** | `GET /posts/{postId}`：非作者对未过审帖 **404**（或协商 403），作者带 Token 可看全量。 |
| P0-6 | **审核权威在服务端** | 客户端 `ForumRepository` 可能存在本地演示用自动过审逻辑；**上线后以服务端为准**，客户端应关闭或改为拉取状态。 |

### P1 — 核心体验

| 序号 | 任务 | 说明 |
|------|------|------|
| P1-1 | **推荐流** | `GET /recommendations` |
| P1-2 | **搭子申请** | `POST /buddy-requests`、`PATCH /buddy-requests/{relationId}`；`FeedViewModel` 含 TODO。 |
| P1-3 | **评论** | 未 `APPROVED` 禁止评论。 |
| P1-4 | **筛选 / 搜索** | `categoryId`、`tag`、`q` 等与 `ForumViewModel` 对齐。 |
| P1-5 | **我的帖子** | `GET /users/me/posts`。 |
| P1-6 | **AI 名片** | `POST /ai/buddy-card` 或并入 `GET /profiles/me`。 |

### P2 — 智能体、内容、社交、偏好

**原生流式**：客户端已按 **`POST /ai/agent/chat/stream`** 发起 SSE，后端需按联调手册 §5.5 实现并与 §2.7 一致。  
其余：`AgentTuning`（`PUT /agent/tuning`）、游戏兴趣、资讯/公告/文旅、关注与私信、AI 发帖草稿、共识卡、聊天偏好持久化等 — **路径与 body 详见联调手册**；客户端多为本地实现，接网时按手册字段扩展 Retrofit。

### P3 — 媒体与安全

上传（头像/帖子媒体）、机审与人审策略，与专项设计一致。

---

## 五、论坛分区 `categoryId`（含电竞文旅）

客户端 **`ForumCategories`** 使用 **英文常量 id**，**禁止用中文作 id**。

| categoryId | App 展示名 |
|------------|------------|
| `recruit` | 峡谷组队 |
| `guide` | 攻略心得 |
| `social` | 潮流水友 |
| `event` | 城市赛事 |
| **`culture`** | **电竞文旅** |

列表查询参数名以 **`元流同频_后端接口联调手册.md`** 为准（如 `categoryId`）；若历史实现使用 `category`，需网关兼容或统一迁移。

---

## 六、文旅与资讯

结构化响应示例、路由 id（如 `esports_culture_detail/{id}`）、`TrendCultureCategory` 枚举等 — **保持与联调手册 §七、§5.5 及 `TX_ku_Android前后端对接说明.md` 一致**。客户端当前可用本地 `EsportsCultureRepository` 等数据源，接口就绪后可整体切换为 `GET /feed/culture` 等。

---

## 七、联调操作清单（后端 + 客户端）

### 7.1 后端准备

1. 部署可访问的 `api/v1` 网关，开放 **登录、刷新**（及注册，若首屏流程需要）、**`GET profiles/me`**、**智能体流式**（若需端到端验证对话）。  
2. 确认 **成功业务码**、登录/刷新 **JSON 键名**（§二）。  
3. 按 **`元流同频_后端接口联调手册.md`** 提供 Swagger/OpenAPI（如 `/docs`、`/openapi.json`）。

### 7.2 客户端准备（工程师）

1. 确认 `AuthRepository.USE_MOCK_AUTH` 与场景一致（真实联调为 **`false`**）。  
2. `ApiConstants.BASE_URL` 指向同一联调环境（已与 `AppContainer` Retrofit 共用）。  
3. 验证：登录 → DataStore 写入 Token → **`GET profiles/me` 20000/200** → 档案页展示；打开智能体页验证 **SSE**（401 时刷新链路）。

### 7.3 验收用例（后端可直接作测试用例）

在 **契约对齐** 完成后，可按联调手册 **「十一、联调检查清单」** 全量执行；并额外增加：

- [ ] 登录请求体在客户端实际为 `passKey` 时，服务端可解析或已约定改名。  
- [ ] 登录/刷新响应字段与 Gson `SerializedName` 一致，或已统一为 camelCase 并改客户端。  
- [ ] 业务成功 `code`：**登录/刷新** 与 `AuthRepository`、`TokenAuthenticator` 判断一致；**`profiles/me`** 与 `ProfileRepository` 双轨约定一致。  
- [ ] `POST /ai/agent/chat/stream` 在带 Bearer 时返回流式事件，客户端 `AgentChatRepository` 可解析 delta/done。  
- [ ] `POST /auth/register` 返回双 Token 后，客户端完成注册页改造并可冷启动恢复会话（待开发）。

更全路径级清单见 **`元流同频_后端接口联调手册.md` §十一**。

---

## 八、参考代码路径（联调定位）

| 模块 | 路径 |
|------|------|
| Base URL 常量 / SSE 路径 | `app/.../core/network/ApiConstants.kt` |
| Retrofit 容器 / OkHttp / SSE 客户端 | `app/.../AppContainer.kt` |
| 登录 / 刷新 API | `app/.../core/network/AuthApiService.kt` |
| 登录业务与 Mock 开关 | `app/.../feature/auth/AuthRepository.kt` |
| 会话持久化 | `app/.../core/prefs/LoginSessionStore.kt` |
| 拦截器 / 刷新 | `AuthInterceptor.kt`、`TokenAuthenticator.kt` |
| 档案聚合 GET | `ProfileApiService.kt`、`ProfileRepository.kt`、`UserMeViewModel.kt` |
| 注册（当前本地） | `LocalAuthRepository.kt`、`RegisterScreen.kt` |
| 智能体 SSE | `AgentChatRepository.kt`、`AgentChatViewModel.kt` |
| 论坛 / 推荐 TODO | `ForumViewModel.kt`、`FeedViewModel.kt`、`ForumRepository.kt` |
| 建档 TODO | `OnboardingViewModel.kt` |
| 智能体本地偏好 | `UserAgentStore.kt` |
| 路由 | `core/navigation/Routes.kt`、`BuddyCardNavHost.kt` |
| OpenAI 兼容开关 | `app/build.gradle.kts` → `OPENAI_COMPAT_*` |

---

## 九、文档维护

- 接口或枚举变更时，请同步：**本文档版本与日期**、**`元流同频_后端接口联调手册.md`**、**`TX_ku_Android前后端对接说明.md`**。  
- **§二「契约对齐」** 应在每次大版本联调前复盘一次，避免手册与代码漂移。

---

**文档结束。**
