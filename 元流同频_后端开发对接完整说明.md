# 《元流同频》后端开发对接完整说明

**产品名称**：**元流同频**（工程目录 `TX_ku`；对外与 `BrandConfig.appDisplayName`、`app_name` 一致）。  
**文档版本**：V1.0（2026-04-13）  
**适用读者**：后端开发、测试、接口设计  
**文档目的**：在完整审查当前 Android 客户端实现的基础上，说明**服务端需要建设的能力、接口形态、数据契约与落地优先级**，便于排期与联调。

---

## 一、审查结论：客户端当前状态

| 维度 | 现状 |
|------|------|
| **HTTP 客户端** | 工程内 **`ApiConstants.BASE_URL`** 已占位；**未接入 Retrofit/OkHttp 业务调用**，业务数据以内存、`SharedPreferences`、本地种子为主。 |
| **鉴权** | 登录/注册为 **`AuthRepository` 内存表**；**无 JWT**；退出仅清会话与部分内存，智能体/游戏兴趣等仍按邮箱键存本地。 |
| **必须替换为服务端的模块** | 认证、画像、论坛、推荐、关注/私信、资讯与文旅策展、智能体对话与配置、游戏兴趣与聊天偏好等（见下文任务清单）。 |
| **合规** | 第三方社区工具，非腾讯官方产品；资讯「官方」指**产品内栏目**，非游戏厂商本体（见 `BrandConfig` / `strings`）。 |

**与下列文档的关系**：

- **`同频搭_API与数据定义文档.md`**：表结构、部分 REST 路径、统一响应壳、错误码示例。  
- **`TX_ku_Android前后端对接说明.md`**：Android 模型字段、审核语义、流式约定、联调清单，**仍以该文档为客户端侧细纲**。  
- **本文**：面向后端的**任务化总览**与**优先级**，并补充代码演进后新增的 **论坛分区 `culture`**、**文旅策展接口** 等。

---

## 二、全局约定（后端必须统一）

### 2.1 基础 URL 与鉴权

- **Base URL**（与代码占位一致，可环境切换）：`https://api.buddycard.com/api/v1/`（开发示例：`http://localhost:8000/api/v1/`、模拟器访问本机 `http://10.0.2.2:8000/api/v1/`）。  
- **Header**：`Authorization: Bearer <access_token>`（与 `ApiConstants.AUTH_HEADER`、`AUTH_PREFIX` 一致）。  
- **公开接口**：注册、登录、刷新 Token（若采用双 Token）等；其余需登录。

### 2.2 统一响应体（建议）

```json
{
  "code": 200,
  "message": "success",
  "data": {}
}
```

- 业务成功建议 **`code == 200`**；失败时 `message` 为人类可读说明，`data` 可为 `null`。  
- 若采用文档 **`同频搭_API与数据定义文档.md`** 中的业务码（如 `20000`），**必须在联调前与客户端书面锁定一种**，避免双套解析。

### 2.3 JSON 命名

- Android 模型为 **camelCase**。若后端使用 **snake_case**，需在客户端为字段加 `@Json` 映射或约定服务端输出 camelCase。

### 2.4 时间与 ID

- 时间：建议 **ISO-8601**，或与客户端现有 `yyyy-MM-dd` / `yyyy-MM-dd HH:mm` **固定一种**。  
- ID：`userId`、`postId`、`commentId`、`relationId` 等建议**字符串全局唯一**。

---

## 三、后端任务总览（按优先级）

### P0 — 上线最小闭环（用户能注册登录、能发帖、能拉公域列表）

| 序号 | 任务 | 说明 |
|------|------|------|
| P0-1 | **注册 / 登录 / JWT** | 返回 `access_token`（及可选 `refresh_token`）；客户端后续请求带 Bearer。 |
| P0-2 | **用户画像** | `POST/PUT /profiles` 或 `PUT /profiles/me` + **`GET /profiles/me`**；与 `Profile`、`AccountSummary` 对齐。 |
| P0-3 | **论坛公域列表** | `GET /posts`：仅返回可对陌生人展示的帖子（与客户端 `Post.isVisibleInPublicForum()` 语义一致，当前即 **`APPROVED`** 等策略允许态）。 |
| P0-4 | **发帖** | `POST /posts`：返回 **`moderationStatus`**、`moderationHint`；新建帖在严格策略下多为 **`PENDING_REVIEW`**。 |
| P0-5 | **帖子详情权限** | `GET /posts/{postId}`：**非作者**对未过审帖 **404/403 且无正文**；**作者**可返回全量含审核字段（与 `PostDetailScreen` 逻辑一致）。 |
| P0-6 | **删除客户端演示逻辑依赖** | 客户端 `ForumRepository` 存在「约 90 秒本地自动过审」**仅用于无后端演示**；上线后 **服务端为审核唯一权威**，客户端应关闭该逻辑或置 `0`，改为轮询/推送。 |

### P1 — 核心产品体验（推荐、搭子申请、评论、搜索筛选）

| 序号 | 任务 | 说明 |
|------|------|------|
| P1-1 | **推荐流** | `GET /recommendations?page=&size=` → `list` + `hasMore`，元素对齐 `Recommendation`。 |
| P1-2 | **搭子申请** | `POST /buddy-requests`、`PATCH /buddy-requests/{relationId}`（`ACCEPT`/`REJECT`）；客户端 `FeedViewModel` 内有 **TODO: POST /buddy-requests**。 |
| P1-3 | **评论** | `GET/POST /posts/{postId}/comments`；**帖子非 `APPROVED` 时禁止评论**（4xx）。 |
| P1-4 | **列表筛选** | 支持 `category`、`tag`、`q`（多关键词空格分隔语义见 `ForumViewModel` / `matchesSearchTokens`）。 |
| P1-5 | **我的帖子** | 建议 `GET /users/me/posts`：作者可见全部审核状态，供「元流档案 · 我的帖子」使用。 |
| P1-6 | **AI 名片** | `POST /ai/buddy-card`；或与 `GET /profiles/me` 捆绑返回 `BuddyCard`。 |

### P2 — 智能体、资讯、文旅、社交与偏好同步

| 序号 | 任务 | 说明 |
|------|------|------|
| P2-1 | **智能体对话** | `POST /ai/agent/chat`（完整回复）；进阶 **`POST /ai/agent/chat/stream`（SSE）** 见 `TX_ku_Android前后端对接说明.md` §3.9。 |
| P2-2 | **AgentTuning 云端** | `GET/PUT` 并入 profile 或独立 `users/me/agent-tuning`；字段与 **`focusScenario`、`avatarStyle` 字符串枚举** 与客户端选项**逐字一致**。 |
| P2-3 | **游戏兴趣** | `GET/PUT /users/me/game-interests`，与 `FollowGameCatalog` 的 **game id** 一致。 |
| P2-4 | **峡谷资讯** | `GET /feed/news?game=&page=&size=`，字段含 **`relatedForumQueries`**（详情页跳转广场搜索）；可选 **`coverUrl`**。 |
| P2-5 | **顶栏公告** | `GET /feed/announcements`，对齐 `FeedAnnouncement`。 |
| P2-6 | **电竞文旅策展** | 见 **§5.5**（`GET /feed/culture` 或与运营 CMS 约定）。 |
| P2-7 | **关注 / 私信 / 用户检索** | `POST/DELETE /follows`、`GET .../following`、`GET /users?q=`、`DM` 相关接口；规则与客户端 **关注后发信、未互关首条限制** 对齐或协商调整。 |
| P2-8 | **发帖 AI 草稿** | `POST /ai/posts`（或等价），与 `AiPostDraft` / 发帖页替换 `buildForumAiDraft` 本地规则。 |
| P2-9 | **共识卡** | `POST /ai/consensus-card`，对齐 `ConsensusCard` / 搭子房叙事。 |
| P2-10 | **聊天偏好** | `GET/PUT /users/me/preferences`：主题 id、沉浸式开关等，对齐 `AgentChatPrefsStore`。 |
| P2-11 | **点赞 / 收藏（多端）** | 若需云端一致：`POST /posts/{id}/like`、`GET/POST /users/me/bookmarks`。 |

### P3 — 媒体与安全

| 序号 | 任务 | 说明 |
|------|------|------|
| P3-1 | **上传** | 预签名 URL 或直传回调；帖子媒体由 `url` 替代本地 `uriString`（见 `PostMedia`）。 |
| P3-2 | **内容审核** | 与 **`论坛与媒体-内容审核（后端设计）.md`**（若存在）对齐：机审、人审、媒体扫描。 |

---

## 四、论坛：分区与枚举（含「电竞文旅」）

客户端 **`ForumCategories`** 中 **`categoryId` 为英文常量**，**禁止用中文作 id**。

| categoryId | App 展示名（文案可改，id 不可变） |
|------------|-----------------------------------|
| `recruit` | 峡谷组队 |
| `guide` | 攻略心得 |
| `social` | 潮流水友 |
| `event` | 城市赛事 |
| **`culture`** | **电竞文旅** |

**后端必须支持**：

- `GET /posts?category=culture&...` 公域列表语义与其它分区一致。  
- `POST /posts` 的 `categoryId` 允许为 **`culture`**。  
- 搜索别名、标签池可参考 `ForumCategories.categorySearchBlob`、`suggestedTagsForCategory("culture")`（含城市漫游、观赛日记、应援穿搭等运营向词）。

> **历史文档勘误**：若旧版接口说明仅列出四个分区，**以本文与代码中的五个分区为准**。

---

## 五、内容域接口补充说明

### 5.1 资讯 `GameNewsItem`

见 **`TX_ku_Android前后端对接说明.md` §2.6**；详情页依赖 **`relatedForumQueries`** 跳转广场。

### 5.2 公告 `FeedAnnouncement`

见 **`GET /feed/announcements`**；用于首页顶栏轮播/列表。

### 5.3 电竞文旅策展（首页「文旅」Tab + 广场嵌入带）

客户端数据源：`EsportsCultureRepository`（本地 JSON 式对象），**可整体替换为接口**。

**建议接口**：`GET /feed/culture`（或 `/feed/esports-culture`）

**建议响应 `data` 结构**（camelCase 示例）：

```json
{
  "cityRoutes": [
    {
      "id": "cr_shanghai",
      "cityName": "上海",
      "regionLabel": "长三角 · 主场氛围",
      "headline": "...",
      "subline": "...",
      "coverGradientStart": 372054144,
      "coverGradientEnd": 4294943809,
      "kplVenueHint": "...",
      "cityCultureHooks": ["...", "..."],
      "itinerary": [
        { "timeLabel": "午后", "title": "...", "body": "..." }
      ],
      "trendTags": ["..."],
      "forumQueries": ["..."],
      "agentPromptSeed": "..."
    }
  ],
  "trendCards": [
    {
      "id": "tr_fashion",
      "title": "...",
      "summary": "...",
      "category": "FASHION",
      "accentGradientStart": 486539264,
      "accentGradientEnd": 4294946816,
      "forumQuery": "...",
      "agentPrompt": "..."
    }
  ]
}
```

**说明**：

- `category`（潮流卡）：与客户端 **`TrendCultureCategory`** 枚举一致：`FASHION`、`FAN_CREATION`、`MUSIC`、`COLLAB`、`LIFESTYLE`。  
- 渐变色字段为 **Long ARGB**，与客户端 `Color(...)` 一致；若有封面图可扩展 `coverUrl`。  
- 详情页路由客户端为 **`esports_culture_detail/{cultureId}`**，后端只需保证 **`cityRoutes[].id` / `trendCards[].id` 全局唯一且稳定**。  
- **合规**：策展文案为第三方社区内容，**赛程/场次以官方为准**；接口层可加免责声明字段或由运营配置。

---

## 六、智能体与错误码

- 对话请求体、流式 **SSE 事件**（`delta` / `done` / `error`）见 **`TX_ku_Android前后端对接说明.md` §2.4、§3.9**。  
- 产品文档中的 AI 超时、格式化失败等业务码（如 **50301 / 50302**）见 **`同频搭_API与数据定义文档.md` §4**；若启用，需与客户端统一展示文案。

---

## 七、联调验收（后端可直接作测试用例）

1. 注册 → 登录 → Token 注入后访问受保护接口 **非 401**。  
2. 建档 → `GET /profiles/me` 与客户端展示一致；可选生成 `BuddyCard`。  
3. 发帖 → 公域列表不出现 `PENDING_REVIEW`；作者「我的帖子」可见；过审后出现。  
4. 非作者拉详情 → 未过审 **无正文**。  
5. 未过审帖 **评论接口失败**。  
6. `categoryId = culture` 发帖与列表筛选正常。  
7. 若已接 **`GET /feed/culture`**，首页文旅 Tab 与广场策展带可拉取同一套 id 打开详情。  
8. 若已接流式智能体：SSE 连续 `delta` 后以 `done` 结束，无永久「输出中」。

更全清单见 **`TX_ku_Android前后端对接说明.md` §3.10**。

---

## 八、参考代码路径（联调定位）

| 模块 | 路径 |
|------|------|
| Base URL | `app/.../core/network/ApiConstants.kt` |
| 路由 | `core/navigation/Routes.kt`、`BuddyCardNavHost.kt` |
| 认证 | `feature/auth/AuthRepository.kt` |
| 论坛仓库与种子 | `feature/forum/ForumRepository.kt`、`ForumCategories.kt` |
| 论坛 VM | `feature/forum/ForumViewModel.kt` |
| 文旅策展 | `feature/feed/EsportsCultureRepository.kt`、`EsportsCityCultureTabContent.kt`、`EsportsCultureDetailScreen.kt` |
| 资讯 | `feature/feed/GameNewsRepository.kt` |
| 推荐与申请 TODO | `feature/feed/FeedViewModel.kt` |
| 智能体 | `feature/chat/AgentChatViewModel.kt`、`AgentTaskRouter.kt`、`UserAgentStore.kt` |
| 画像 | `core/model/Profile.kt`、`BuddyCard.kt` |

---

## 九、文档维护

- 接口或枚举变更时，请 **同步** 更新：  
  - 本文档版本号与日期；  
  - **`TX_ku_Android前后端对接说明.md`**；  
  - **`同频搭_API与数据定义文档.md`**（或后续更名为统一 API 手册）。  
- 审核、媒体、风控细节以专项设计文档为准，避免与论坛接口语义冲突。

---

**文档结束。**
