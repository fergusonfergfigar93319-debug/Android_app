# 《元流同频》后端接口联调手册

**文档性质**：面向前端工程师的完整联调参考手册  
**版本**：V1.0（2026-04-13）  
**后端环境**：已完成两轮开发与本地真实联调，可进入前端接入阶段  
**Swagger UI**：服务启动后访问 `http://localhost:8000/api/v1/openapi.json`（或 `/docs`）

---

## 一、全局约定

### 1.1 Base URL

| 环境 | 地址 |
|---|---|
| 本地开发 | `http://localhost:8000/api/v1` |
| Android 模拟器访问本机 | `http://10.0.2.2:8000/api/v1` |
| 生产 | `https://api.buddycard.com/api/v1` |

### 1.2 统一响应结构

所有接口统一返回以下结构（**业务码以 `code` 字段为准，不以 HTTP Status 为准**）：

```json
{
  "code": 20000,
  "message": "success",
  "data": {}
}
```

> ⚠️ **重要**：`code` 字段值为 `20000` 表示成功，**不是** `200`。请前端拦截器统一判断 `code == 20000`。

### 1.3 业务状态码字典

| code | HTTP Status | 含义 | 前端建议处理 |
|---|---|---|---|
| `20000` | 200 | 请求成功 | 正常渲染 data |
| `40001` | 400 | 参数校验失败 | Toast 提示 message 内容 |
| `40004` | 404 | 资源不存在 | 提示"内容不存在" |
| `40100` | 401 | Token 缺失或过期 | 清本地缓存，跳登录页 |
| `40300` | 403 | 无权限 | Toast 提示"权限不足" |
| `50000` | 500 | 服务器内部错误 | Toast 提示"服务器开小差了" |
| `50301` | 503 | AI 智能体生成超时 | 提示"智能体正在沉思中，请稍后重试" |

### 1.4 鉴权方式

所有需要登录的接口在 Header 中携带：

```
Authorization: Bearer <access_token>
```

> **公开接口**（无需 Token）：注册、登录、`GET /feed/news`、`GET /feed/announcements`、`GET /feed/culture`、`GET /posts`（广场列表）

### 1.5 字段命名规范

- 服务端统一输出 **camelCase**，前端直接使用无需转换
- 时间字段为 **ISO-8601** 格式字符串
- ID 字段全部为 **UUID 字符串**格式

---

## 二、认证模块（Auth）

### 2.1 注册

```
POST /auth/register
```

**Request Body：**
```json
{
  "email": "user@example.com",
  "password": "min6chars",
  "nickname": "暴走萝莉",
  "avatarUrl": "https://..."
}
```

**Response data：**
```json
{
  "accessToken": "eyJ...",
  "user": {
    "id": "uuid-string",
    "email": "user@example.com",
    "regNickname": "暴走萝莉"
  }
}
```

### 2.2 登录

```
POST /auth/login
```

**Request Body：**
```json
{
  "email": "user@example.com",
  "password": "yourpassword"
}
```

**Response data**：同注册，返回 `accessToken` + `user`。

> **Token 有效期**：7 天（`ACCESS_TOKEN_EXPIRE_MINUTES = 60 * 24 * 7`）。过期后重新登录获取新 Token。

---

## 三、用户画像模块（Profile）

### 3.1 创建/更新画像（建档）

```
POST /profiles
PUT /profiles/me
```

两个路由逻辑完全相同（Upsert），均可用于建档与更新。**推荐首次建档用 `POST`，后续更新用 `PUT`。**

**Request Body（所有字段均可选，提供哪些更新哪些）：**
```json
{
  "nickname": "暴走萝莉",
  "avatarUrl": "https://...",
  "bio": "个性签名",
  "cityOrRegion": "广州",
  "preferredGames": ["王者荣耀", "英雄联盟"],
  "rank": "钻石",
  "activeTime": ["20:00-24:00", "周末全天"],
  "mainRoles": ["射手", "打野"],
  "playStyle": "激进打架",
  "target": "冲分",
  "voicePref": "必须语音",
  "noGos": ["不沟通", "甩锅"]
}
```

**Response data**：返回更新后的画像对象（字段同上）。

### 3.2 获取我的聚合配置（冷启动）

```
GET /profiles/me
```

**Response data：**
```json
{
  "profile": { ...画像字段 },
  "buddyCard": {
    "cardId": "uuid",
    "tags": ["下路杀神", "话痨"],
    "declaration": "只要辅助保得好，对面高地是我家。",
    "rules": ["打野多来下", "可以菜但不能怂"],
    "proPersonaLabel": "冷静型战术大脑",
    "baseMatchScore": 85
  },
  "agentTuning": { ...智能体配置 }
}
```

> `buddyCard` 和 `agentTuning` 在未生成/配置时为 `null`。

---

## 四、论坛社区模块（Forum）

### 4.1 发帖

```
POST /posts
```

**Request Body：**
```json
{
  "categoryId": "recruit",
  "title": "【钻石/晚间】寻硬辅",
  "content": "帖子正文...",
  "tags": ["寻辅助", "晚间档"],
  "mediaAttachments": []
}
```

> **`categoryId` 枚举（共5个，严格区分大小写）**：
> - `recruit` — 峡谷组队
> - `guide` — 攻略心得
> - `social` — 潮流水友
> - `event` — 城市赛事
> - `culture` — 电竞文旅
>
> 传入非法值将返回 `40001` 参数错误。

**Response data**：新帖对象，注意 **`moderationStatus` 初始值为 `PENDING_REVIEW`**，帖子在后台审核通过后变为 `APPROVED`，广场列表才会显示。

### 4.2 获取广场帖子列表（公域）

```
GET /posts?categoryId=&page=1&size=20
```

- 公开接口，无需 Token
- **仅返回 `APPROVED` 状态的帖子**
- 支持按 `categoryId` 过滤（参数名 `categoryId`，camelCase）

**Response data：**
```json
{
  "list": [ ...post对象数组 ],
  "hasMore": true
}
```

### 4.3 获取帖子详情

```
GET /posts/{postId}
```

> **权限规则（重要）**：
> - **未过审帖**（`PENDING_REVIEW`/`REJECTED`）：
>   - 作者本人携带 Token 访问 → 返回 200 全量（含 `moderationStatus`、`moderationHint`）
>   - 非作者或匿名 → 返回 **404**（不暴露帖子存在性）
> - **已过审帖**：所有人可访问

**Response data：**
```json
{
  "id": "uuid",
  "categoryId": "recruit",
  "title": "...",
  "content": "...",
  "tags": [],
  "moderationStatus": "APPROVED",
  "moderationHint": null,
  "authorId": "uuid",
  "authorName": "暴走萝莉",
  "authorAvatar": "https://...",
  "replyCount": 5,
  "likeCount": 12,
  "isPinned": false,
  "createdAt": "2026-04-13T09:00:00Z",
  "comments": [ ...前3条评论 ]
}
```

### 4.4 获取我的帖子列表

```
GET /users/me/posts?page=1&size=20
```

- **需要 Token**，匿名返回 401
- 返回当前用户**全部审核状态**的帖子（含 `PENDING_REVIEW`，供发帖页状态展示使用）

### 4.5 评论

```
GET /posts/{postId}/comments?page=1&size=20   # 获取评论列表
POST /posts/{postId}/comments                  # 发表评论（需要Token）
```

> ⚠️ **帖子必须处于 `APPROVED` 状态才允许评论**，否则返回 `403`。

**POST 评论 Request Body：**
```json
{
  "content": "评论内容"
}
```

### 4.6 点赞 / 收藏

```
POST /posts/{postId}/like       # 点赞（重复点赞返回 400）
POST /posts/{postId}/bookmark   # 收藏 Toggle（已藏则取消）
```

---

## 五、AI 智能体模块（AI）

### 5.1 生成专属名片

```
POST /ai/buddy-card
```

- 无需 Request Body，后端自动读取当前用户画像
- 耗时 2-5 秒（AI 调用，前端建议展示 Loading）

**Response data：**
```json
{
  "tags": ["夜猫子", "冲分党", "稳健"],
  "declaration": "大局观极佳的老将，绝不压力...",
  "rules": ["连跪两把休息", "只喷操作不喷人"],
  "proPersonaLabel": "冷静型战术大脑"
}
```

### 5.2 AI 生成招募帖草稿（不落库）

```
POST /ai/posts
```

**Request Body：**
```json
{
  "intent": "寻一位护阵型硬辅，要求脾气好"
}
```

**Response data：**
```json
{
  "title": "【钻石/晚间】绝活射手寻硬辅",
  "content": "帖子正文...",
  "tags": ["寻辅助", "晚间挡"]
}
```

### 5.3 AI 生成双人共识卡

```
POST /ai/consensus-card
```

**Request Body：**
```json
{
  "targetUserId": "目标用户UUID"
}
```

**Response data：**
```json
{
  "matchScore": 92,
  "matchReasons": ["时段完美重合", "风格互补"],
  "advice": "建议初期先文字沟通战术...",
  "icebreakerSuggestion": "嘿，看你也是射手位，要不要一起排？"
}
```

### 5.4 智能体对话（同步）

```
POST /ai/agent/chat
```

**Request Body：**
```json
{
  "message": "今天打了好几把，感觉很丧",
  "messages": [
    {"role": "user", "content": "上一条"},
    {"role": "assistant", "content": "AI回复"}
  ]
}
```

> `message` 和 `messages` 至少提供一个。若同时提供，优先使用 `messages` 历史（多轮模式）。

**Response data：**
```json
{
  "reply": "AI完整回复文本",
  "messageId": "uuid"
}
```

### 5.5 智能体对话（流式 SSE）

```
POST /ai/agent/chat/stream
```

**Request Body**：同同步版。  
**Response**：`Content-Type: text/event-stream`

SSE 事件格式（每行以 `data: ` 开头的 JSON）：

```
data: {"type": "delta", "text": "流式文字片段"}
data: {"type": "delta", "toolCall": "正在检索信息..."}
data: {"type": "done", "messageId": "uuid", "finishReason": "stop"}
data: {"type": "error", "code": 50301, "message": "错误信息"}
```

> **对接要点**：
> - 监听 `type == "delta"` → 追加到对话气泡
> - 监听 `type == "done"` → 结束打字机效果，存储 `messageId`
> - 监听 `type == "error"` → 展示错误提示，清空"输出中"状态
> - **流一定会以 `done` 或 `error` 结尾**，不存在永久"输出中"

---

## 六、推荐与搭子申请模块（Feed）

### 6.1 获取推荐搭子流

```
GET /recommendations?page=1&size=10
```

- 数据来自 Redis 缓存（O(1) 响应，约 50ms 内）
- 若缓存为空（新用户），返回空列表并提示"正在为你匹配中"

**Response data：**
```json
{
  "list": [
    {
      "userId": "uuid",
      "nickname": "国服辅王",
      "avatarUrl": "https://...",
      "matchScore": 95,
      "matchReasons": ["完美互补：您打射手，ta打辅助", "时间高度重合"],
      "conflict": "沟通差异：ta偶尔不方便开麦",
      "advice": "建议初期先文字沟通...",
      "card": { ...BuddyCard对象 }
    }
  ],
  "hasMore": true
}
```

### 6.2 手动刷新推荐缓存

```
POST /recommendations/refresh
```

触发后台异步计算，即时返回 `200`，稍后再拉取列表。

### 6.3 发起搭子申请

```
POST /buddy-requests
```

**Request Body：**
```json
{
  "targetUserId": "目标用户UUID",
  "message": "看了你的名片，感觉很合拍！"
}
```

> 重复发送待处理申请返回 `400`。

### 6.4 处理搭子申请（同意/拒绝）

```
PATCH /buddy-requests/{relationId}
```

**Request Body：**
```json
{
  "action": "ACCEPT"
}
```

> `action` 枚举：`ACCEPT`（同意） / `REJECT`（拒绝）  
> 非申请接收方操作返回 `403`，重复处理返回 `400`

---

## 七、资讯与文旅模块（Feed Content）

### 7.1 峡谷速递资讯

```
GET /feed/news?game=王者荣耀&page=1&size=10
```

- 公开接口，无需 Token
- `game` 参数可选，按游戏名过滤

**单条资讯字段：**
```json
{
  "id": "uuid",
  "gameName": "王者荣耀",
  "topicTag": "版本速报",
  "authorName": "官方运营",
  "title": "野区节奏调整上线...",
  "summary": "简短摘要",
  "detailBody": "完整正文",
  "relatedForumQueries": ["打野节奏", "版本理解"],
  "commentCount": 18,
  "likeCount": 63,
  "isOfficial": true,
  "timeLabel": "2小时前",
  "coverUrl": "https://..."
}
```

> `relatedForumQueries` 用于详情页跳转广场搜索，直接作为搜索关键词传入广场搜索接口。

### 7.2 顶栏公告轮播

```
GET /feed/announcements
```

**Response data：**
```json
{
  "list": [
    { "id": "uuid", "title": "本周组队活动开启", "body": "完成3次双排可获得..." }
  ]
}
```

### 7.3 电竞文旅策展

```
GET /feed/culture
```

- 公开接口，无需 Token
- 供首页文旅 Tab 和广场策展带使用

**Response data：**
```json
{
  "cityRoutes": [
    {
      "id": "cr_shanghai",
      "cityName": "上海",
      "regionLabel": "长三角 · 主场氛围",
      "headline": "从主场馆到潮流街区...",
      "subline": "适合赛前热身、观赛打卡...",
      "coverGradientStart": 372054144,
      "coverGradientEnd": 4294943809,
      "kplVenueHint": "KPL 上海主场周边适合...",
      "cityCultureHooks": ["赛场打卡", "联名快闪"],
      "itinerary": [
        { "timeLabel": "午后", "title": "主场周边集合", "body": "..." }
      ],
      "trendTags": ["主场氛围", "城市漫游"],
      "forumQueries": ["上海观赛", "主场打卡"],
      "agentPromptSeed": "帮我规划一条适合上海观赛日的路线"
    }
  ],
  "trendCards": [
    {
      "id": "tr_fashion",
      "title": "应援穿搭实验室",
      "summary": "把主队配色、联名单品和城市街头感混搭...",
      "category": "FASHION",
      "accentGradientStart": 486539264,
      "accentGradientEnd": 4294946816,
      "forumQuery": "应援穿搭",
      "agentPrompt": "帮我做一套适合观赛日的应援穿搭方案"
    }
  ]
}
```

> **详情页路由**：客户端路由 `esports_culture_detail/{id}`，`id` 字段全局唯一且稳定（`cr_shanghai`、`tr_fashion` 等），可直接用于跳转。  
> **`category` 枚举**：`FASHION` / `FAN_CREATION` / `MUSIC` / `COLLAB` / `LIFESTYLE`

---

## 八、社交模块（Social）

### 8.1 搜索用户

```
GET /users?q=搜索昵称
```

**Response data**：返回用户列表，每项包含 `id`、`nickname`、`avatarUrl`、`bio`。

### 8.2 关注 / 取消关注

```
POST /follows          Body: { "targetUserId": "uuid" }   # 关注
DELETE /follows/{targetUserId}                             # 取消关注
GET /users/me/following                                    # 获取我关注的人
```

### 8.3 私信（WebSocket 实时通信）

**连接地址**：
```
ws://localhost:8000/api/v1/ws/dm?token=<access_token>
```

**发送消息（客户端 → 服务端）：**
```json
{
  "action": "send_message",
  "payload": {
    "toUserId": "目标用户UUID",
    "text": "消息内容"
  }
}
```

**接收消息（服务端 → 客户端推送）：**
```json
{
  "event": "new_message",
  "data": {
    "id": "uuid",
    "fromUserId": "uuid",
    "text": "消息内容",
    "sentAt": "2026-04-13T09:00:00Z"
  }
}
```

> **未互关规则**：未互相关注时，每人只能向对方**发送一条**消息，超出返回 `403`。

### 8.4 离线私信拉取

```
GET /dm/threads                           # 私信会话列表（最近联系人）
GET /dm/{peerUserId}/messages?page=1&size=20   # 拉取与某人的聊天记录（自动标记已读）
```

---

## 九、智能体配置模块（AgentTuning）

```
PUT /agent/tuning
```

**Request Body（字段均可选）：**
```json
{
  "emotionTone": "活跃",
  "humorMix": "多",
  "socialEnergy": "热情",
  "replyLength": "LONG",
  "customPersonaScript": "自定义人设剧本...",
  "tabooNotes": "不能说的话...",
  "agentDisplayNameOverride": "我的搭子小助手"
}
```

> 该接口与 `GET /profiles/me` 返回的 `agentTuning` 字段对应，修改后下次发起对话即生效。

---

## 十、媒体上传

```
POST /uploads/avatar    # 上传头像
POST /uploads/post      # 上传帖子附图（返回 url 用于发帖时的 mediaAttachments）
```

---

## 十一、联调检查清单

前端接入时建议按以下顺序验证：

- [ ] `POST /auth/register` → 返回 `accessToken`
- [ ] `POST /auth/login` → 返回 `accessToken`
- [ ] 携带 Token 访问 `GET /profiles/me` → 非 401
- [ ] `POST /profiles` 建档 → 成功
- [ ] `POST /ai/buddy-card` → 返回 `tags`、`declaration`、`rules`
- [ ] `GET /recommendations` → 返回列表（新用户可能为空）
- [ ] `POST /posts` 发帖 → `moderationStatus` 为 `PENDING_REVIEW`
- [ ] 作者查自己 `PENDING_REVIEW` 帖子详情 → 200
- [ ] 非作者查同一帖子 → 404
- [ ] `GET /posts` 广场列表 → 不出现 `PENDING_REVIEW`
- [ ] `POST /posts/{id}/comments` 对未过审帖评论 → 403
- [ ] `GET /users/me/posts` → 能看到自己的 `PENDING_REVIEW` 帖
- [ ] `POST /buddy-requests` → 申请成功
- [ ] `PATCH /buddy-requests/{id}` body `ACCEPT` → 成功；重复操作 → 400
- [ ] `POST /ai/agent/chat` → 返回 `reply` + `messageId`
- [ ] `POST /ai/agent/chat/stream` → SSE 流式 `delta` 后以 `done` 结束
- [ ] `GET /feed/news` → 无 Token 可访问
- [ ] `GET /feed/culture` → 无 Token 可访问，data 含 `cityRoutes` 和 `trendCards`
- [ ] 传入 `categoryId: "culture"` 发帖 → 成功
- [ ] 传入非法 `categoryId` → 返回 `40001`

---

## 十二、注意事项与已知限制

| 事项 | 说明 |
|---|---|
| **推荐缓存** | 新用户无缓存，`GET /recommendations` 返回空列表，需先手动调用 `POST /recommendations/refresh` 触发计算 |
| **资讯/公告内容** | 当前为 Mock 静态数据，后续接入运营 CMS 后替换，接口结构不会变化 |
| **文旅策展** | 当前为 Mock 静态数据，`id` 字段稳定可作为路由参数 |
| **帖子审核** | 后台审核任务为 3 秒模拟延迟后自动过审，生产环境替换为真实机审接口 |
| **AI 接口耗时** | 文本生成约 2-8 秒，建议前端所有 AI 接口统一展示 Loading 状态 |
| **WebSocket 鉴权** | Token 通过 URL Query 参数传递：`?token=<access_token>` |

---

*联调过程中如有接口字段疑问，请以 Swagger UI（`/docs`）为最终参考，或联系后端确认。*
